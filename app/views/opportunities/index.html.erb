
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Opportunities</h1>
    <%= link_to 'New opportunity', new_opportunity_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>

  <div id="opportunities" class="min-w-full">
    <%= render @opportunities %>
  </div>

  <%= button_to "Log out", destroy_user_session_path, method: :delete, form: { data: { turbo_confirm: "Are you sure?" } } %>
</div>
